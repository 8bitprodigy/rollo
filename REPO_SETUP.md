# Repository Setup

Suggested Git commands:

```bash
git init
git add .
git commit -m "Initial Rollo language specification repository"
git branch -M main
git remote add origin <your-github-repo-url>
git push -u origin main
```
